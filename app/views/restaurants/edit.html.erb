<div class='container'>
  <h2>Whats changed?</h2>
  <%= form_for @restaurant, remote: true do |f| %>
    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name, autofocus: true %>
    </div>

    <div class="field">
      <%= f.label :description %><br />
      <%= f.text_area :description, class: "materialize-textarea" %>
    </div>

    <div class="actions">
      <%= f.submit "Make changes", class: 'waves-effect waves-light btn' %>
    </div>

  <% end %>
</div>