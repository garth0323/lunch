<div class="section">
  <div class="container">
    <h2 class="header">Reviews</h2>
  </div>
</div>

<div class='container'>
  <%= render 'restaurant' %>
  <%= render 'new_review' %>
  <%= link_to 'Change Details of Restaurant', edit_restaurant_path(@restaurant) %>
</div>